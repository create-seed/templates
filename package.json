{
  "name": "templates",
  "description": "Project templates for create-seed.",
  "private": true,
  "repository": {
    "name": "create-seed/templates",
    "type": "git",
    "url": "https://github.com/create-seed/templates"
  },
  "scripts": {
    "generate": "bun x create-seed@${CREATE_SEED_TAG:-latest} registry generate",
    "validate": "bun x create-seed@${CREATE_SEED_TAG:-latest} registry validate --fail-on-warning"
  }
}
